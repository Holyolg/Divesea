{
  "description": "",
  "devDependencies": {
    "json-server": "^0.17.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run json-server": "npx json-server --watch db.json"
}
}